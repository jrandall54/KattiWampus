Place sample input/output pairs here. Example structure:

- `cases/` — directory of numbered input files `01.in`, `01.out`, etc.
- `verify.py` — (optional) script to compare program output to expected.
